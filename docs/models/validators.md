# Validators

Validators for this measurement. Use structured ValidatorSpec objects with operator and expected_value. Legacy string format (e.g. "x >= 3") is also accepted and stored as expression.


## Supported Types

### `List[models.RunCreateValidators]`

```python
value: List[models.RunCreateValidators] = /* values here */
```

### `List[str]`

```python
value: List[str] = /* values here */
```

